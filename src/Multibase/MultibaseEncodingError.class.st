"
!Error raised by ${class:name=MultibaseEncoder}$

Raised only during decoding. Indicates an illegal character in the input.
"
Class {
	#name : #MultibaseEncodingError,
	#superclass : #Error,
	#category : #'Multibase-Base'
}
